#include <stdio.h>

int main() {
    int n, key, found = 0;
    scanf("%d", &n);
    int reg[n];
    for (int i = 0; i < n; i++) scanf("%d", &reg[i]);

    scanf("%d", &key);

    for (int i = 0; i < n; i++) {
        if (reg[i] == key) {
            printf("Registration number %d found at position %d\n", key, i + 1);
            found = 1;
            break;
        }
    }
    if (!found) printf("Registration number %d not found\n", key);
    return 0;
}

